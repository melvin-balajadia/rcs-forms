# Site config for Marilao — sourced by deploy.sh, and discovered by the
# deploy-test.yml/deploy-prod.yml workflows (one file per site = one matrix entry).
# Not secret — just domains/ports. Actual DB/JWT secrets live in
# server/.env.marilao.test / server/.env.marilao.prod (git-ignored).

SITE_LABEL=Marilao

TEST_CLIENT_DOMAIN=mqfdform.royalecoldstorage.com.ph
TEST_CLIENT_PORT=1002
TEST_SERVER_PORT=8082
TEST_MYSQL_PORT=3309

PROD_CLIENT_DOMAIN=rcsmqfdform.royalecoldstorage.com.ph
PROD_CLIENT_PORT=2002
PROD_SERVER_PORT=8087
PROD_MYSQL_PORT=3309
