# Site config for Taytay — sourced by deploy.sh, and discovered by the
# deploy-test.yml/deploy-prod.yml workflows (one file per site = one matrix entry).
# Not secret — just domains/ports. Actual DB/JWT secrets live in
# server/.env.taytay.test / server/.env.taytay.prod (git-ignored).

SITE_LABEL=Taytay

TEST_CLIENT_DOMAIN=tqfdform.royalecoldstorage.com.ph
TEST_CLIENT_PORT=1004
TEST_SERVER_PORT=8084
TEST_MYSQL_PORT=3310

PROD_CLIENT_DOMAIN=rcstqfdform.royalecoldstorage.com.ph
PROD_CLIENT_PORT=2004
PROD_SERVER_PORT=8089
PROD_MYSQL_PORT=3310
